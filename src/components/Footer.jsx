import SocialLinks from './SocialLinks';

const Footer = () => {
  const component = (
    <footer>
      <SocialLinks
        section="footer"
      />
    </footer>
  );

  return component;
};

export default Footer;
